#include <stdio.h>
#include <stdlib.h>
int main(int argc, char const *argv[]){
	char a;
	/*a=getchar();
	putchar(a);*/
	putchar(getchar());
	putchar(getchar());
	putchar(getchar());
	return 0;
}